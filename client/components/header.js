import React from 'react';

const Header = ()=> {
    return (
        <nav className="nav navbar-default">
            <div className='navbar-header'>
                <a ahref='*' className='navbar-brand'>ShortenMyLink</a>
            </div>
        </nav>
    );
};

export default Header;